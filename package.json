{
  "name": "name-layout-generator",
  "version": "0.1.0",
  "description": "PC desktop application for automatic name layouts with transparent PNG export.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/renderer.js && node --check src/layoutEngine.js && node scripts/check-conflicts.js && npm test"
  },
  "keywords": [
    "layout",
    "printing",
    "sablon",
    "png-export"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron": "^42.0.0"
  }
}
